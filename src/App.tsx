// Placeholder shell. Phase 2 replaces this with the questionnaire + results +
// export view (pure renderers over content.json). The scoring engine and
// content pipeline (Phase 1) are complete and tested.
export function App() {
  return <main>Agent Adoption Advisor</main>;
}
